<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Login | Cavalos Genéricos</title>

  <script src="https://cdn.twind.style" crossorigin></script>
  <script type="module" src="/js/twind-setup.js"></script>

  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700;800;900&display=swap" rel="stylesheet">

  <style>
    :root {
      --verde-cg: #00BF6F;
      --verde-hover: #009e58;
      --gold-1: #D4AF37;
      --gold-2: #b99125;
      --gold-3: #f7e27f;
      --preto-cg: #1a1a1a;
    }

    body { font-family: 'Montserrat', sans-serif; }

    .gold-shine {
      background: linear-gradient(90deg, var(--gold-1), var(--gold-2), var(--gold-1));
      background-size: 200%;
      animation: shine 4s linear infinite;
    }
    @keyframes shine {
      0% { background-position: 200% center; }
      100% { background-position: -200% center; }
    }

    .card-responsive {
      padding: 2rem;
      max-width: 100%;
    }

    h1 {
      font-size: 2rem;
    }

    input, button {
      font-size: 1rem;
    }

    img.h-36 {
      height: 9rem;
    }

    @media (max-width: 768px) {
      .card-responsive {
        padding: 1.8rem;
      }

      h1 { font-size: 1.8rem; }
      img.h-36 { height: 7rem; }
      input, button { font-size: 0.95rem; }
    }

    @media (max-width: 480px) {
      .card-responsive {
        padding: 1.5rem;
      }

      h1 { font-size: 1.6rem; }
      img.h-36 { height: 6rem; }
      input, button { font-size: 0.9rem; padding: 0.8rem 1rem; }
    }

    @media (max-width: 360px) {
      h1 { font-size: 1.4rem; }
      img.h-36 { height: 5rem; }
      input, button { font-size: 0.85rem; padding: 0.7rem 0.9rem; }
    }

    @media (max-height: 700px) {
      .card-responsive {
        padding: 1.5rem;
      }

      img.h-36 { height: 6.5rem; }
      h1 { font-size: 1.6rem; }
    }
  </style>

</head>

<body class="min-h-screen bg-gradient-to-br from-black via-gray-900 to-black flex items-center justify-center p-4 overflow-hidden relative">

  <div class="absolute inset-0 opacity-25">
    <div class="absolute inset-0 bg-gradient-to-t from-black via-transparent to-black z-10"></div>
    <img src="https://images.unsplash.com/photo-1553284965-83fd3e82fa5a?auto=format&fit=crop&q=80" 
         alt="Cavalo em movimento" class="w-full h-full object-cover">
  </div>

  <div class="relative z-20 w-full max-w-md">
    <div class="backdrop-blur-xl bg-white/10 border border-white/20 rounded-3xl shadow-2xl overflow-hidden transform transition-all duration-500 hover:scale-[1.01] card-responsive">

      <div class="bg-gradient-to-r from-black via-gray-900 to-black p-8 text-center border-b border-gold-1/40" style="border-bottom: 1px solid var(--gold-1)">
        <img src="/img/F.png" alt="Cavalos Genéricos" class="h-36 mx-auto mb-4 drop-shadow-xl">
        <h1 class="text-3xl md:text-4xl font-black text-white tracking-tight">ÁREA RESTRITA</h1>
        <p class="text-gray-300 mt-2 text-sm font-medium">Acesso exclusivo para equipe e revendedores</p>
      </div>

      <div class="p-7 md:p-10">
        <form action="/admin/login" method="POST" class="space-y-7">

          <div>
            <label class="block text-sm font-bold text-gold-1 mb-2 tracking-wide">Usuário</label>
            <div class="relative">
              <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none">
                <svg class="w-5 h-5 text-gold-1/70" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14"></path>
                </svg>
              </div>

              <input type="text" name="usuario" required autocomplete="username"
                     class="w-full pl-12 pr-5 py-4 bg-white/10 border border-white/20 rounded-xl focus:ring-2 focus:ring-gold-1 focus:border-gold-1 text-white placeholder-gray-400 transition-all duration-300"
                     placeholder="Seu usuário">
            </div>
          </div>

          <div>
            <label class="block text-sm font-bold text-gold-1 mb-2 tracking-wide">Senha</label>
            <div class="relative">
              <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none">
                <svg class="w-5 h-5 text-gold-1/70" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 11c0-2.67 2.24-4 5-4s5 1.33 5 4-2.24 4-5 4-5-1.33-5-4zM5 19h14"></path>
                </svg>
              </div>

              <input type="password" name="senha" required autocomplete="current-password"
                     class="w-full pl-12 pr-5 py-4 bg-white/10 border border-white/20 rounded-xl focus:ring-2 focus:ring-gold-1 focus:border-gold-1 text-white placeholder-gray-400 transition-all duration-300"
                     placeholder="Sua senha">
            </div>
          </div>

          <% if (typeof mensagem !== 'undefined') { %>
            <div class="bg-red-600/20 border border-red-400 text-red-300 px-5 py-4 rounded-xl text-center font-semibold animate-pulse">
              <%= mensagem %>
            </div>
          <% } %>

          <button type="submit"
                  class="w-full gold-shine text-black font-extrabold text-lg py-5 rounded-xl shadow-xl hover:shadow-2xl hover:-translate-y-1 transition-all duration-300 flex items-center justify-center gap-3 group">
            <span>ENTRAR NA ÁREA RESTRITA</span>
            <svg class="w-6 h-6 group-hover:translate-x-2 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M9 5l7 7-7 7"></path>
            </svg>
          </button>

        </form>

        <div class="mt-10 text-center">
          <p class="text-xs text-gray-400">
            © <%= new Date().getFullYear() %> Cavalos Genéricos — Todos os direitos reservados
          </p>
        </div>
      </div>
    </div>
  </div>

  <div class="absolute top-10 left-5 w-48 h-48 rounded-full bg-gold-1/20 blur-3xl opacity-20 animate-pulse"></div>
  <div class="absolute bottom-10 right-5 w-72 h-72 rounded-full bg-gold-1/20 blur-3xl opacity-20 animate-pulse delay-1000"></div>

</body>
</html>
